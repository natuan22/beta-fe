import React from 'react'
import { memo } from "react";

const CurrentPayoutRatio = (props) => {

    return (
        <>
            <div>CurrentPayoutRatio</div>
        </>
    )
}

export default memo(CurrentPayoutRatio)